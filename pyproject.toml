[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["mutants"]

[tool.ruff.lint]
select = ["E", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [".venv", "mutants", "__pycache__"]
